{
  "name": "@charliewilco/esm-defaults",
  "license": "Unlicense",
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "node ./scripts/build",
    "postbuild": "tsc --declaration --emitDeclarationOnly",
    "example": "vite example --config example/vite.config.js",
    "test": "jest",
    "vercel-build": "vite build example --outDir ../public --config example/vite.config.js"
  },
  "devDependencies": {
    "esbuild": "^0.12.15",
    "jest": "^27.0.6",
    "prettier": "^2.3.2",
    "ts-jest": "^27.0.3",
    "typescript": "^4.3.5",
    "vite": "^2.4.2"
  },
  "prettier": {
    "printWidth": 95,
    "singleQuote": false,
    "semi": true,
    "bracketSpacing": true,
    "jsxBracketSameLine": true
  },
  "jest": {
    "preset": "ts-jest"
  }
}
